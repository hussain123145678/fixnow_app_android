package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Elegant Pakistani Emerald & White Trust theme colors
val BrandEmerald     = Color(0xFF0F9F68) // Vibrant Trust Emerald Green
val LightGreenAccent = Color(0xFFE2F6EC) // Friendly soft mint accent container
val BrandCharcoal    = Color(0xFF1C1D1F) // Rich near-black for body text
val SolidNavy        = Color(0xFF0F2636) // Deep slate accent
val CardSlate        = Color(0xFFF1F5F9) // Warm tile grey background
val AccentAmber      = Color(0xFFF59E0B) // Cozy gold for stars and alerts

val LightBackground  = Color(0xFFFAFAFA) // Pure high-contrast clean white background
val LightSurface     = Color(0xFFFFFFFF) // Crisp white card surfaces
val DarkBackground   = Color(0xFFFAFAFA) // Forcing light theme backgrounds throughout
val DarkSurface      = Color(0xFFFFFFFF) // Forcing light theme surfaces throughout

val TextPrimaryLight = Color(0xFF1C1B1F) // Rich near-black text
val TextSecLight     = Color(0xFF49454F) // Slate secondary body text
val TextPrimaryDark  = Color(0xFF1C1B1F) // Forcing high-contrast text
val TextSecDark      = Color(0xFF49454F) // Forcing readable secondary text

// Category visual palettes from the theme spec
val CategoryElectricBg = Color(0xFFE8DEF8)
val CategoryElectricText = Color(0xFF21005D)
val CategoryAcBg = Color(0xFFD1E4FF)
val CategoryAcText = Color(0xFF001D36)
val CategoryPlumbingBg = Color(0xFFC2EAD0)
val CategoryPlumbingText = Color(0xFF062112)
val CategoryCleaningBg = Color(0xFFFFDAD6)
val CategoryCleaningText = Color(0xFF410002)

